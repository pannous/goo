class tee{
#		int x
#		int y
		x int
		y int
		tee(int x, int y) {
				this.x = x
				this.y = y
		}
		sum() int { return x + y }
		display() { print("x: " + x + ", y: " + y) }
}
t := tee(3, 4)
t.display()
print("Sum: " + t.sum())
return
