import "fmt"
import "os"
path:="."


files, err := os.ReadDir(path)
if err { panic("Error reading directory ${path}") }

for _, file := range files {
#	printf(file.Name())
	fmt.Println(file.Name())
	return // "debug_op.goo" OK
}
