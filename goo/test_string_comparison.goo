check "a" == 'a'  // invalid operation: "a" == 'a' (mismatched types untyped string and rune)
check "a" == 'a' // todo allow string/rune comparison!

// Test only basic methods that don't need imports
check "a"+"1" == "a1"
check "a"+1 == "a1" // invalid operation: "a" + 1 (mismatched types untyped string and untyped int)
check "hi".first() == "h"
check "hi".last() == "i"
