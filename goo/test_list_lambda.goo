import "slices"

# depends on test_list_typed.goo and map function test_list_map.goo
xs := [1, 2, 3]

check xs.apply(x=>x*2)  == []int{2, 4, 6}
check [1, 2, 3].apply(x=>x*2)  == [2, 4, 6]

check xs.filter(x => x%2==1) == [1, 3]
# check xs.reduce((a,b)=>a+b, 0) == 6

print("All OK")

