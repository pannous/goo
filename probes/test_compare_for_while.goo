// Compare for vs while with same structure
myMap := map[string]int{"a": 1, "b": 2}

printf("Testing for:\n")
for key, value in myMap {
    printf("  %s = %d\n", key, value)
}

printf("Testing while:\n")
while key, value in myMap {
    printf("  %s = %d\n", key, value)
}