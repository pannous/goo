// Panic context try statement test

import "strconv"

def testPanicContext() {
    try val := strconv.Atoi("invalid")
    printf("Got value: %d", val)
}

try {
    testPanicContext()
} catch e {
    printf("Caught panic: %v", e)
}