package main

class Point {
    x int
    y int
    sum() int { 
        return self.x + self.y 
    }
    display() { 
        printf("x: %d, y: %d\n", self.x, self.y) 
    }
}

func main() {
    t := Point{x: 3, y: 4}
    t.display()
    printf("Sum: %d\n", t.sum())
}