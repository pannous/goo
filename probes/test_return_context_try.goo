// Return context try statement test

import "strconv"

def testReturnContext() error {
    try val := strconv.Atoi("invalid")
    printf("Got value: %d", val)
    return nil
}

err := testReturnContext()
if err != nil {
    printf("Got error: %v", err)
} else {
    printf("No error")
}