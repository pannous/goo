import "strings"
import "slices"

// These cases fail from the original test - let's isolate them
printf("Testing failing cases:\n")

// Direct composite literals in check statements (from original)
check 2 in [1,2,3]                    // FAILS
check "hello" in ["hello", "world"]   // FAILS

// Variables in check statements (from original)
x := 2
y := [1, 2, 3]
check x in y                          // FAILS

text := "hello world"
check "hello" in text                 // FAILS