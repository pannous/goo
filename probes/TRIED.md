2025-02-14: Attempted `git merge upstream/master` after adding upstream remote (https://github.com/golang/go.git). Merge produced extensive conflicts across core compiler/type system files, so approach stalled before resolution.
2025-02-14: Tried merging `upstream/release-branch.go1.26` as a smaller step, but upstream does not publish that branch yet; merge command failed before doing any work.
