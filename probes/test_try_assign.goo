// Basic try statement test

import "strconv"

def testBasicTry() {
// todo allow both syntaxes
    try i := "a" as int
    val := try "a" as int
// todo allow throwing away value (hard?)
    try "a" as int
    printf("This should not be reached")
}

testBasicTry()
printf("Basic try test done")